import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.capstone.angular2026',
  appName: 'capstone-angular-2026',
  webDir: 'www',
};

export default config;
